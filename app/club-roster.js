(function (w) {
  "use strict";

  /* Live membership is Supabase (Ski Paradise Cleveland, email join + admin approval).
     This file is not loaded on the hosted Mini. Empty so a leftover cache cannot
     seed the old 30-name roster onto the club board. */
  w.LAKE_CLUB_NAME = "Ski Paradise Cleveland";
  w.LAKE_SEED_GEN = "invite-only-1";
  w.LAKE_CLUB_ROSTER = [];
})(window);
